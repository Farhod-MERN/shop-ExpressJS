<body>
    
<div class="container">
<main class="container">
  <div class="p-4 p-md-5 mb-4 rounded text-bg-dark">
    <div class="col-md-6 px-0">
      <h1 class="display-4 fst-italic">All book for you ❤️</h1>
      <p class="lead my-3">A book is a written or printed collection of words that people read, usually bound together within a thick cover.</p>
      <p class="lead mb-0"><a href="#" class="text-white fw-bold">Continue reading...</a></p>
    </div>
  </div>

  <div class="row mb-2">
    {{#each products}}
      <div class="col-md-6">
      <div class="row g-0 bg-white border-3 border border-primary shadow rounded overflow-hidden flex-md-row mb-4 shadow-sm h-md-250 position-relative">
        <div class="position-relative col p-2 d-flex flex-column position-static">
          <strong class="d-inline-block mb-2 text-primary">{{username}}</strong>
          <h3 class="mb-0">{{title}}</h3>
          <div class="mb-1 text-muted"><small>{{getCut createdAt}}</small></div>
          <p class="card-text mb-auto">{{sliceText description}} ...</p>
          <div class="mb-1 d-flex ">
            <span class="avatar mx-2">{{getFullNameFirstElem user.firstName user.lastName}}</span>
            <div >
              <p class="mt-4 m-0">{{user.firstName}} {{user.lastName}}</p>
            </div>
           </div>
            <div class="position-absolute top-0 end-0 btn-group btn-group-md" role="group" aria-label="Basic outlined example">
            <a href="/product/{{_id}}" type="button" class="btn btn-success"><i class="bi bi-info-circle"></i></a>
            {{#if @root.token}}
            {{#ifequal user._id @root.userId}}
              <a href="edit-product/{{_id}}" type="button" class="btn btn-info"><i class="bi bi-pencil-square"></i></a>
              <form action="/delete-product/{{_id}}" method="post">
              <button type="submit" class="btn btn-danger"><i class="bi bi-trash3"></i></button>
              </form>
            {{/ifequal}}
            {{/if}}
        </div>
        </div>
        <div class="col-auto d-none d-lg-block">
          <img src={{image}} alt="image" class="bd-placeholder-img" width="200" height="250">
          {{!-- <svg class="bd-placeholder-img" width="200" height="250" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="Placeholder: Thumbnail" preserveAspectRatio="xMidYMid slice" focusable="false"><title>Placeholder</title><rect width="100%" height="100%" fill="#55595c"></rect><text x="50%" y="50%" fill="#eceeef" dy=".3em">Thumbnail</text></svg> --}}
        </div>

      </div>
    </div>
    {{/each}}
  </div>

  <div class="row g-5">
    <div class="col-md-8">
      <h3 class="pb-4 mb-4 fst-italic border-bottom">
        From the Our comunity
      </h3>

      <article class="blog-post">
        <h2 class="blog-post-title mb-1">Sample blog post</h2>
        <p class="blog-post-meta">January 1, 2021 by <a href="#">Farhod</a></p>

        <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit. Enim doloremque commodi reprehenderit numquam explicabo eaque!</p>
        <hr>
        <p>This is some additional paragraph placeholder content. It has been written to fill the available space and show how a longer snippet of text affects the surrounding content. We'll repeat it often to keep the demonstration flowing, so be on the lookout for this exact same string of text.</p>
        <h2>Details</h2>
        <p>This is an example blockquote in action:</p>
        <blockquote class="blockquote">
          <p>Quoted text goes here.</p>
        </blockquote>
        <p>This is some additional paragraph placeholder content. It has been written to fill the available space and show how a longer snippet of text affects the surrounding content. We'll repeat it often to keep the demonstration flowing, so be on the lookout for this exact same string of text.</p>
        <h3>Create post by step by step</h3>
        <p>This is some additional paragraph placeholder content. It's a slightly shorter version of the other highly repetitive body text used throughout. This is an example unordered list:</p>
        <ul>
          <li>First list item</li>
          <li>Second list item with a longer description</li>
          <li>Third list item to close it out</li>
        </ul>
        <p>And this is an ordered list:</p>
        <ol>
          <li>First list item</li>
          <li>Second list item with a longer description</li>
          <li>Third list item to close it out</li>
        </ol>
        <p>And this is a definition list:</p>        
      </article>
    </div>

    <div class="col-md-4">
      <div class="position-sticky" style="top: 2rem;">
        <div class="p-4 mb-3 bg-light rounded">
          <h4 class="fst-italic">About us</h4>
          <p class="mb-0">Customize this section to tell your visitors a little bit about your publication, writers, content, or something else entirely. Totally up to you.</p>
        </div>

        <div class="p-4 mb-5">
          <h4 class="fst-italic">For more</h4>
          <ol class="list-unstyled">
            <li><a href="https://github.com/Farhod-MERN">GitHub</a></li>
            <li><a href="https://www.linkedin.com/in/farhod-shuhratovich/">Linkedin</a></li>
            <li><a href="https://farhod.vercel.app/">Farhod</a></li>
          </ol>
        </div>

      </div>
    </div>
  </div>

</main>

</body>
<!-- Remove the container if you want to extend the Footer to full width. -->

<!-- End of .container -->

{{!-- import "/public/index.css"
{{#if products.length }}
<h3>All Books for you ! ❤️</h3>
<div class="books" style="width: 100%;">
  {{#each products}}
    <div class="m-2 bg-white">
    <div class="bookCard position-relative">
      <div class="mobileCard d-flex g-0 border rounded overflow-hidden shadow-lg h-md-250 position-relative">
        <div class="col p-4 d-flex flex-column position-static">
          <div class="d-flex justify-content-between">
            <strong class="d-inline-block mb-2 text-primary">{{username}}</strong>
            <span><strong>$</strong>{{price}}.00</span>
          </div>
          <h4  class="mb-1">{{title}}</h4>
          <p class="card-text mb-auto">{{description}}</p>
           <div class="mb-1 d-flex ">
            <span class="avatar mx-2">{{getFullNameFirstElem user.firstName user.lastName}}</span>
            <div >
              <p class="mt-3 m-0">{{user.firstName}} {{user.lastName}}</p>
              <p class="m-0"><small>{{getCut createdAt}}</small></p>
            </div>
           </div>
        </div>
        <div class="d-block">
         <img class="bookImage" src={{image}} alt="image">
        </div>
      </div>
        <div class="position-absolute top-0 end-0 btn-group btn-group-md" role="group" aria-label="Basic outlined example">
            <a href="/product/{{_id}}" type="button" class="btn btn-success"><i class="bi bi-info-circle"></i></a>
            {{#if @root.token}}
            {{#ifequal user._id @root.userId}}
              <a href="edit-product/{{_id}}" type="button" class="btn btn-info"><i class="bi bi-pencil-square"></i></a>
              <form action="/delete-product/{{_id}}" method="post">
              <button type="submit" class="btn btn-danger"><i class="bi bi-trash3"></i></button>
              </form>
            {{/ifequal}}
            {{/if}}
        </div>
    </div>
   
    </div>
  {{/each}}
</div>
{{else}}
    <h3>No products yet</h3>
{{/if}} --}}